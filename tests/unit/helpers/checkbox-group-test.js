import { checkboxGroup } from 'remoteapp/helpers/checkbox-group';
import { module, test } from 'qunit';

module('Unit | Helper | checkbox group');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkboxGroup([42]);
  assert.ok(result);
});
