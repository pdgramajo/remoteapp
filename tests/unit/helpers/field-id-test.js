import { fieldId } from 'remoteapp/helpers/field-id';
import { module, test } from 'qunit';

module('Unit | Helper | field id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fieldId([42]);
  assert.ok(result);
});
